services:
  nrpe:
    build: .
    image: nrpe:debian
    user: nagios
    init: true
    restart: unless-stopped
    volumes: 
      - ./etc/nrpe.cfg:/etc/nagios/nrpe_local.cfg:ro
      - /:/rootfs:ro
    ports:
      - 5666:5666